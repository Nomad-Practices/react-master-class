import { Wrapper, Box } from './components/styled'
import { motion } from 'framer-motion'

function App() {
   return (
      <Wrapper>
         <Box />
         <motion.div></motion.div>
      </Wrapper>
   )
}

export default App
